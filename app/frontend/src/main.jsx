import React,{useEffect,useState} from 'react';import{createRoot}from'react-dom/client';import'./style.css';
function App(){const[tasks,setTasks]=useState([]);const[title,setTitle]=useState('');const load=()=>fetch('/api/tasks').then(r=>r.ok?r.json():[]).then(setTasks);useEffect(load,[]);const add=e=>{e.preventDefault();if(!title.trim())return;fetch('/api/tasks',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title})}).then(load).then(()=>setTitle(''))};return <main><h1>Cloud Tasks</h1><p>Production platform demo application</p><form onSubmit={add}><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Add a task"/><button>Add task</button></form><ul>{tasks.map(t=><li key={t.id}><span>{t.title}</span><small>{t.status}</small></li>)}</ul></main>}createRoot(document.getElementById('root')).render(<App/>);
